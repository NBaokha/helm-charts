apiVersion: v2
name: grafana
description: Grafana, wrapping the upstream grafana-community chart.
type: application

# Version of this wrapper. appVersion is the Grafana we run, pinned via image.tag.
version: 1.0.0
appVersion: 12.4.0

# Pinned to 9.4.5: the old Chart.yaml
dependencies:
  - name: grafana
    version: 9.4.5
    repository: https://grafana-community.github.io/helm-charts
